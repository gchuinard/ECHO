<nav class="navbar navbar-expand-lg navbar-dark echoNavbar" style="font-size: 1rem;" font-family: "Roboto, Helvetica, Arial, sans-serif" ;>
	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo03" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>

	<div class="collapse navbar-collapse" id="navbarTogglerDemo03">
		<a class="echo-bars-navbar" href="#">
			<svg xmlns="http://www.w3.org/2000/svg" x="0px" y="0px" width="18" height="18" viewbox="0 0 50 50" style=" fill:#fff;">
				<path d="M 0 7.5 L 0 12.5 L 50 12.5 L 50 7.5 Z M 0 22.5 L 0 27.5 L 50 27.5 L 50 22.5 Z M 0 37.5 L 0 42.5 L 50 42.5 L 50 37.5 Z"></path>
			</svg>
		</a>
		<ul class="navbar-nav mt-2 mt-lg-0 d-flex align-items-center">
			<li class="nav-item active echo-link-navbar">
				<a class="nav-link" href="{{frontUrl}}">Home</a>
			</li>
			<li class="nav-item active echo-link-navbar">
				<a class="nav-link" href="{{ path('tag_list') }}">Tags</a>
			</li>
			{% if app.user != "" %}

				<li class="nav-item active echo-link-navbar">
					<a class="nav-link" href="{{frontUrl}}/profile">Mon profil</a>
				</li>
				<li class="nav-item active echo-link-navbar">
					<a class="nav-link" href="{{ path('conv_user_list') }}">Mes conversations</a>
				</li>
				{% if is_granted('ROLE_ADMIN') %}
					<li class="nav-item active echo-link-navbar">
						<a class="nav-link" href="{{ path('admin') }}">Administration</a>
					</li>
				{% endif %}

			</ul>
			<div class="my-2 ml-auto my-lg-0 d-flex align-items-center echo-logout-part">
				<span class="echo-user-navbar">
					Bonjour
					{{ app.user.username }}</span>
				<a href="{{frontUrl}}/logout" class="echoLogout">
					<i class="far fa-times-circle"></i>
				</a>
			</div>
		{% else %}

			<li class="nav-item active echo-link-navbar">
				<a class="nav-link" href="{{frontUrl}}/login">Se connecter</a>
			</li>
			<li class="nav-item active echo-link-navbar">
				<a class="nav-link" href="{{frontUrl}}/signup">S'inscrire</a>
			</li>
		</ul>
	{% endif %}
</div></nav>
